{
  "name": "chinesezodiacyear.com",
  "version": "2.0.0",
  "type": "module",
  "description": "Encyclopaedia, directory, and news platform for Chinese zodiac, BaZi, feng shui, and traditional Chinese culture.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve --port=8080",
    "dev": "npx @11ty/eleventy --serve --port=8080",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev",
    "infra:bootstrap": "./scripts/bootstrap-d1-r2.sh",
    "infra:d1:create": "wrangler d1 create czy-main",
    "infra:r2:create": "wrangler r2 bucket create czy-reports",
    "infra:list": "wrangler d1 list && wrangler r2 bucket list",
    "infra:migrations:check": "bash ./scripts/check-migrations.sh",
    "infra:migrations:apply:local": "wrangler d1 migrations apply ${D1_DATABASE_BINDING:-DB} --local",
    "infra:migrations:apply:remote": "wrangler d1 migrations apply ${D1_DATABASE_BINDING:-DB} --remote"
    "infra:list": "wrangler d1 list && wrangler r2 bucket list"
  },
  "keywords": [
    "chinese-zodiac",
    "bazi",
    "fengshui",
    "eleventy"
  ],
  "author": "Chinese Zodiac Year",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-img": "^6.0.4",
    "esbuild": "^0.27.4",
    "vitest": "^4.1.0",
    "wrangler": "^4.71.0"
  }
}
